<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Add Dino</title>
  </head>
  <body>
    <h1>Fill Out The Form Below</h1>
    <div class="input-wrapper">
      <form class="dino" action="/{{dino.id}}" method="{{method}}">
        <input type="text" name="name" value="{{dino.name}}" placeholder="Add Name" autofocus='on'>
        <input type="text" name="color" value="{{dino.color}}" placeholder="Add Colors">
        <input type="text" name="diet" value="{{dino.diet}}" placeholder="Add Diet">
        <input type="text" name="imageurl" value="{{dino.imageurl}}" placeholder="Add Image">
        <button type="submit" class="Submit" name="button" data-id="{{id}}">Submit</button>
      </form>
    </div>
    <script src="/main.js"></script>
  </body>
</html>
